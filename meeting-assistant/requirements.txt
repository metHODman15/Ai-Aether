fastapi>=0.136.0,<1.0.0
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
openai==1.55.0
anthropic==0.49.0
sounddevice==0.5.1
numpy==2.1.3
soundfile==0.12.1
websockets==13.1
pdfplumber==0.11.4
python-docx==1.1.2
python-multipart==0.0.17

# Optional — only required when WHISPER_BACKEND=local
# faster-whisper runs Whisper entirely on-device (no OpenAI calls for transcription).
# Install with:  pip install faster-whisper
# faster-whisper>=1.0.3
cryptography==42.0.8
httpx==0.27.2

# Salesforce Hosted MCP Server transport. Pulls in httpx-sse + sse-starlette.
mcp>=1.0.0
