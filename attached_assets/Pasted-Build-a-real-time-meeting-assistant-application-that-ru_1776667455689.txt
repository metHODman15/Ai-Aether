Build a real-time meeting assistant application that runs entirely locally on my machine. The application should continuously capture audio from my microphone and transcribe it using OpenAI's Whisper API. Use Claude API exclusively for context management—analyze the transcribed conversation flow to detect when the discussion topic shifts to a new subject. Query Salesforce using extracted business entities like customer names, deal amounts, and stages through their REST API with my credentials to fetch matching records and aggregated metrics. Render real-time pie charts and line graphs using Chart.js or Plotly to visualize the Salesforce data on a web-based dashboard. Keep all graphs and visualizations pinned to the screen as long as the current conversation topic remains active. When Claude's context management detects a topic shift, clear the existing charts and render new visualizations based on the updated Salesforce query for the new topic.
Additionally, the application should support uploading minutes of meeting documents in formats like PDF, Word, or plain text. When a document is uploaded, parse it and treat every individual conversation message or exchange within it as a discrete unit. For each unit, extract relevant business entities, query Salesforce, and generate a corresponding chart or graph so that every single conversation message in the uploaded document has its own visual representation tied to real Salesforce data. These visualizations should be displayed sequentially alongside the parsed conversation, creating a visual narrative of the entire meeting grounded in CRM data.
The entire backend logic should be Python-based on my Laptop, with a simple HTML and JavaScript frontend that runs locally on my laptop as well. Include proper error handling, API key management through environment variables, and ensure all dependencies are listed in a requirements.txt file for reproducibility. The project should be structured to be easily cloned from GitHub and run with zero configuration beyond setting environment variables for my Salesforce credentials and API keys.